# Copy this to .env and fill in your actual values
# NEVER commit .env to git!

# ========================================
# CORE REQUIRED (Minimum to run ClawdBot)
# ========================================
GROQ_API_KEY=your-groq-api-key-here
SUPABASE_URL=your-supabase-project-url
SUPABASE_ANON_KEY=your-supabase-anon-key

# ========================================
# OPTIONAL - Extended Features
# ========================================

# Alternative LLM Providers
OPENROUTER_API_KEY=your-openrouter-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# Slack Integration (for notifications)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_APP_TOKEN=xapp-your-slack-app-token

# Gmail Integration (for email confirmations)
GMAIL_EMAIL=your-email@gmail.com
GMAIL_PASSWORD=your-app-password
GMAIL_CLIENT_ID=your-gmail-client-id
GMAIL_CLIENT_SECRET=your-gmail-client-secret

# CAPTCHA Solving (for auto-apply)
CaptchaKey=your-2captcha-api-key

# ========================================
# ENVIRONMENT LOADING METHODS
# ========================================

# Method 1: .env file (recommended)
# Copy this file to .env and fill in values

# Method 2: Windows Environment Variables
# Set these in User Environment Variables:
# - GROQ_API_KEY
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SLACK_BOT_TOKEN (if using Slack)
# - CaptchaKey (if using auto-apply)

# Method 3: PowerShell Profile
# Add to your PowerShell profile ($PROFILE):
# $env:GROQ_API_KEY = "your-key"
# $env:SUPABASE_URL = "your-url"
# $env:SUPABASE_ANON_KEY = "your-key"

# ========================================
# HOW CLAWDBOT LOADS ENVIRONMENT
# ========================================

# 1. First checks os.environ (current session)
# 2. Then checks Windows User Environment Variables
# 3. Falls back to .env file if python-dotenv installed
# 4. Raises error if required keys missing

# ========================================
# API KEY SOURCES
# ========================================

# Groq (LLM - FREE): https://console.groq.com/keys
# Supabase (Database): https://supabase.com/dashboard → Project → Settings → API
# OpenRouter (LLM): https://openrouter.ai/keys
# 2Captcha (CAPTCHA): https://2captcha.com/
# Slack (Notifications): https://api.slack.com/apps
